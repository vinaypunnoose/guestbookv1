apiVersion: v1
kind: Pod
metadata:
  name: guestbook-pod
  labels:
    name: guestbook-pod
    app: guestbook-app
spec:
  containers:
    - name: guestbook-app
      image: kubernetes/guestbook:v2
      ports:
        - containerPort: 3000
